你是一名资深桌面应用工程师。请帮我从零开发一个本地桌面软件，暂名 PhotoMind。

先不要写代码。请先复述需求，给出技术方案、依赖、目录结构、数据库表、分阶段开发计划，并问我 5 个关键问题。等我确认后再开始写代码。

【技术栈】
Electron + React + TypeScript + Vite + SQLite + React Flow + Zustand + Tailwind CSS。
优先 Windows，能打包成 exe，兼容 macOS 更好。
软件完全离线运行，不修改、不移动、不删除用户原图。

【核心功能一：照片查看与管理】
1. 首页有“导入文件夹”按钮，选择本地文件夹后递归扫描图片，支持 jpg/jpeg/png/webp/gif/bmp。
2. 首页以缩略图网格展示照片，并按自定义分类分组。未分类照片放在“未分类”分组。
3. 支持自定义分类：新建、重命名、删除、排序、设置颜色。
4. 一张照片支持多分类。首页按分类分组时，多分类照片可以出现在多个分组中，同时保留“全部照片”视图。
5. 点击照片进入查看器：
   - 显示大图；
   - 鼠标滚轮缩放，拖拽平移，双击复位；
   - 支持上一张、下一张按钮；
   - 支持键盘左右方向键切换；
   - 显示文件名、路径、当前序号；
   - 可以给当前照片选择分类，支持多选，也可以取消分类；
   - 分类后返回首页，首页分组自动更新。
6. 删除照片时只删除软件内记录，不删除本地原文件。

【核心功能二：思维导图联想画布】
1. 顶部导航有“照片库 / 联想画布 / 设置”。
2. 联想画布是一个空白无限画布，支持平移、缩放。
3. 左侧显示照片库缩略图，可以把照片拖入画布，生成图片节点。
4. 节点可以拖动、缩放、删除、复制；节点显示图片缩略图和标题。
5. 节点之间可以连线：从节点边缘拖出连线到另一个节点。
6. 连线可以选中、删除、添加文字标签。
7. 画布支持保存、加载、自动保存。重启软件后，节点位置、图片、连线、标签都能恢复。
8. 可选：支持导出画布为 PNG 和 JSON。

【数据存储】
使用 SQLite 保存：
- photos：id、path、filename、created_at
- categories：id、name、color、sort
- photo_categories：photo_id、category_id
- maps：id、name、created_at、updated_at
- map_nodes：id、map_id、photo_id、x、y、width、height、title
- map_edges：id、map_id、source_node_id、target_node_id、label

缩略图缓存到 appData 目录，不要修改原图。

【UI 要求】
界面简洁现代，支持深色/浅色模式。照片多时要懒加载，不能明显卡顿。

【验收标准】
1. 选择含 20 张图片的文件夹，首页出现 20 张缩略图。
2. 点击图片进入查看器，能滚轮缩放、拖拽、左右键切换。
3. 新建分类“旅行”，把照片加入后，返回首页能在“旅行”分组看到。
4. 重启软件后，分类和照片记录仍存在。
5. 打开联想画布，从照片库拖 3 张照片进去，移动、连线、保存，重启后恢复。
6. 全程不修改、不删除本地原图。

【工作方式】
每次只实现一个阶段，确保项目可运行。
每阶段完成后，告诉我运行命令、测试步骤、本阶段改了什么。
如果需求有歧义，先问我，不要自己猜。